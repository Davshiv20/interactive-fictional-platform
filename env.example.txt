# Copy this file to .env for local development
# Vite requires env vars to be prefixed with VITE_

# HuggingFace API Token (Optional but recommended for higher rate limits)
# Get from: https://huggingface.co/settings/tokens
# Free to use! The app works without a token but may have rate limits
VITE_HUGGINGFACE_API_TOKEN=

